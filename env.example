# Django settings (example)
DEBUG=True
SECRET_KEY=change-me-in-production

# If you deploy, list your domain(s) here, comma-separated (no quotes)
ALLOWED_HOSTS=127.0.0.1,localhost

# CORS origins (comma-separated, no trailing slashes)
CORS_ALLOWED_ORIGINS=http://localhost:8100,http://127.0.0.1:8100

# Database (SQLite by default). For Postgres use a DATABASE_URL pattern instead.
# DATABASE_URL=postgres://user:pass@host:5432/dbname
